"""Room Service route stubs.

In the proxy twin, Twirp RPC calls are handled by proxy.py.
This module exists for structural completeness and could hold
emulation logic if the twin ever needs to operate without an upstream.
"""
